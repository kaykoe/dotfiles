#!/usr/bin/env bash

{{ $docker_exists := lookPath "docker" }}

{{ if $docker_exists -}}
set -e

sudo systemctl enable --now docker
sudo usermod -G docker -a "$USER"
newgrp docker
sudo systemctl restart docker
{{ end -}}
